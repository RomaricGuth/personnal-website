# Brevo — contact form email delivery (https://www.brevo.com)
# 1. Create a free account
# 2. Add a verified sender (noreply@romaricguth.com) or authenticate the domain
# 3. Create an API key (SMTP & API > API Keys) and paste it below
BREVO_API_KEY=

# Optional overrides (sensible defaults are used if unset)
# CONTACT_TO=contact@romaricguth.com
# CONTACT_FROM=noreply@romaricguth.com
